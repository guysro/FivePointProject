<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State when the button is pressed -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/start_button_pressed"/>
            <corners android:radius="100dp"/>
            <padding
                android:left="16dp"
                android:top="10dp"
                android:right="16dp"
                android:bottom="10dp"/>
            <stroke
                android:width="2dp"
                android:color="@color/start_button_pressed"/>
        </shape>
    </item>

    <!-- Default state (not pressed) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/start_button"/>
            <corners android:radius="100dp"/>
            <padding
                android:left="16dp"
                android:top="10dp"
                android:right="16dp"
                android:bottom="10dp"/>
            <stroke
                android:width="2dp"
                android:color="@color/start_button"/>
        </shape>
    </item>
</selector>
